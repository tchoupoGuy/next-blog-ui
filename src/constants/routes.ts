export enum Route {
  HOME = "/",
  EXPERIENCE = "/experience",
  ACHIEVEMENTS = "/achievements",
  ABOUT = "/about",
  BLOGS = "/blogs",
  TESTIMONIALS = "/testimonials",
  CONTACTS = "/contacts",
}
